# EasyCamp — Roadmap VibeCoding по роли уборщицы

Статус: **DRAFT на утверждение**  
Дата: 2026-02-27  
Цель: перевести модуль уборщицы из «просмотра выездов» в полноценный процесс исполнения задач с качественным контролем.

---

## 0) Что фиксируем сразу (границы MVP cleaner v2)

### В scope
- Сущность задач уборки (отдельно от booking)
- Чеклист с обязательными пунктами качества
- Статусы выполнения задачи (pending→accepted→in_progress→done)
- Отказ + эскалация
- SLA по молчанию (нет ответа в дедлайн)
- Фото-отчёт после уборки
- Учёт расходников (моющее, губки, тряпки и пр.)
- Уведомления админам о нехватке расходников

### Out of scope (позже)
- ML/оценка качества по фото
- Геолокация уборщицы
- Авторасчёт зарплаты на основе задач
- Мобильное web-приложение для персонала

---

## 1) Продуктовая модель (как должно работать)

1. В системе есть бронь с выездом на дату D.
2. Job создаёт `CleaningTask` на дату D (idempotent).
3. Уборщица получает задачу и выбирает: принять/отклонить.
4. Если приняла — может нажать «Начала уборку».
5. Перед «Завершить» обязана заполнить чеклист и приложить фото.
6. Если нет ответа в SLA или отказ — админам уходит эскалация.
7. Если отмечена нехватка расходников — админам уходит алерт на закупку.

---

## 2) Чеклист качества (обязательные чекпоинты)

> Эти пункты должны быть в боте явно, с чекбоксами/кнопками.  
> `done` запрещён, пока не заполнены обязательные пункты.

### Блок A: Спальные места
- [ ] Заправила все кровати
- [ ] Заменила постельное бельё (после выезда)
- [ ] Проверила комплектность (простыня/пододеяльник/наволочки)

### Блок B: Санузел
- [ ] Унитаз и сиденье вымыты (дезинфекция)
- [ ] Раковина и смесители вымыты
- [ ] Душ/ванна вымыты
- [ ] Зеркало протёрто
- [ ] Туалетная бумага/мыло пополнены

### Блок C: Кухня и посуда
- [ ] Вся посуда вымыта и высушена
- [ ] Стол/столешница протёрты
- [ ] Плита/микроволновка очищены
- [ ] Холодильник проверен и очищен от остатков
- [ ] Мусор вынесен, пакет заменён

### Блок D: Полы и поверхности
- [ ] Полы пропылесосила
- [ ] Полы вымыла
- [ ] Пыль протёрта (тумбы/подоконники/полки)

### Блок E: Расходники и инвентарь
- [ ] Обновила/долила моющее средство
- [ ] Обновила губки
- [ ] Обновила тряпки/салфетки
- [ ] Проверила перчатки/пакеты/чистящие средства
- [ ] Отметила, что нужно докупить (если не хватает)

### Блок F: Финал
- [ ] Проветрила помещение
- [ ] Выключила свет/технику, закрыла окна/двери
- [ ] Прикрепила 3 фото: спальня, санузел, кухня

### Правила валидации завершения
- Все пункты обязательных блоков A-D-F должны быть отмечены
- Минимум 3 фото обязательно
- Если в блоке E отмечен дефицит — создать `SupplyAlert`
- Только после этого кнопка «✅ Завершить уборку» активна

---

## 3) Дата-модель и миграции

## 3.1 Таблица cleaning_tasks
Поля:
- `id` PK
- `booking_id` FK bookings.id
- `house_id` FK houses.id
- `assigned_to_user_id` FK users.id nullable
- `scheduled_date` date (обычно check_out)
- `status` enum:
  - `pending`
  - `accepted`
  - `declined`
  - `in_progress`
  - `done`
  - `escalated`
  - `cancelled`
- `confirm_deadline_at` datetime
- `accepted_at` datetime nullable
- `started_at` datetime nullable
- `completed_at` datetime nullable
- `decline_reason` text nullable
- `escalated_at` datetime nullable
- `notes` text nullable
- `created_at`, `updated_at`

Индексы:
- `(scheduled_date, status)`
- `(assigned_to_user_id, status)`
- unique `(booking_id)` для idempotent-создания 1 задачи на выезд

## 3.2 Таблица cleaning_task_checks
(результаты чеклиста)
- `id` PK
- `task_id` FK cleaning_tasks.id
- `code` string (например `beds_made`, `dishes_cleaned`)
- `label` string
- `is_required` bool
- `is_checked` bool
- `checked_at` datetime nullable

Индексы:
- `(task_id, code)` unique

## 3.3 Таблица cleaning_task_media
- `id` PK
- `task_id` FK cleaning_tasks.id
- `telegram_file_id` string
- `media_type` enum (`photo`)
- `uploaded_by_user_id` FK users.id
- `created_at`

## 3.4 Таблица supply_alerts
- `id` PK
- `task_id` FK cleaning_tasks.id
- `house_id` FK houses.id
- `reported_by_user_id` FK users.id
- `items_json` text/json (например: ["моющее","губки","тряпки"])
- `status` enum (`new`, `in_progress`, `resolved`)
- `created_at`, `resolved_at`

---

## 4) Машина статусов (строгие переходы)

Разрешённые переходы:
- `pending -> accepted | declined | escalated | cancelled`
- `accepted -> in_progress | declined | escalated | cancelled`
- `in_progress -> done | escalated | cancelled`
- terminal: `done | declined | escalated | cancelled`

Правила:
- `done` только при валидации чеклиста+фото
- `declined` требует причину (preset + optional текст)
- `escalated` может выставить job/SLA или админ вручную

---

## 5) Telegram UX для уборщицы

## 5.1 Главное меню cleaner
- `📌 Мои задачи сегодня`
- `🗓 Мои задачи на неделю`
- `📚 История`
- `🔄 Обновить`

## 5.2 Карточка задачи
Показывает:
- Домик, дата, гость, телефон, окно выезда
- Текущий статус
- Дедлайн подтверждения

Кнопки по статусу:
- pending: `✅ Принять`, `❌ Отказаться`
- accepted: `🚿 Начать уборку`
- in_progress: `☑️ Чеклист`, `📸 Фото`, `✅ Завершить`

## 5.3 Callback contract
- `cleaner:task:view:{task_id}`
- `cleaner:task:accept:{task_id}`
- `cleaner:task:decline:{task_id}`
- `cleaner:task:start:{task_id}`
- `cleaner:task:check:{task_id}:{check_code}`
- `cleaner:task:photo:{task_id}`
- `cleaner:task:done:{task_id}`

---

## 6) SLA и эскалации

## Параметры (env/config)
- `CLEANING_CONFIRM_WINDOW_MIN=30`
- `CLEANING_START_WINDOW_MIN=120` (опционально)
- `CLEANING_FORCE_DONE_DEADLINE=16:00` (опционально)
- `CLEANING_REQUIRE_PHOTOS=3`

## Job: `cleaning_sla_monitor`
Запуск каждые 5 минут:
- если task `pending` и дедлайн подтверждения прошёл → `escalated`
- если task `accepted` и слишком долго без `in_progress` → alert админу
- если task `in_progress` и после дедлайна без `done` → alert

Антиспам:
- для каждого правила 1 алерт на задачу (флаг/метка времени)

---

## 7) Админ-панель (минимум)

MVP в Telegram admin:
- Список проблемных задач (`escalated`, `overdue`)
- Действия:
  - переназначить уборщицу
  - закрыть вручную
  - пометить закупку resolved

Следующий шаг (web): таблица задач с фильтрами.

---

## 8) Поэтапный план с чекпоинтами (VibeCoding)

## Этап 1 — Schema foundation
Задачи:
- добавить модели + alembic миграции
- enum статусов cleaner
- базовые CRUD-методы сервиса

Чекпоинт:
- миграции проходят
- task создаётся/читается через smoke script

## Этап 2 — Task generation + idempotency
Задачи:
- генератор задач из выездов на дату
- заменить прямые cleaner-notify от booking на task-based

Чекпоинт:
- дубли не создаются при повторном запуске job

## Этап 3 — Cleaner interactions
Задачи:
- кнопки принять/отказаться/начать
- статусные переходы через service layer

Чекпоинт:
- happy-path работает end-to-end

## Этап 4 — Checklist + media gate
Задачи:
- хранение чеклиста в БД
- переключение пунктов
- приём 3 фото
- блокировка done без валидации

Чекпоинт:
- done недоступен без обязательных чеков + фото

## Этап 5 — Supplies alerts
Задачи:
- форма/кнопки «не хватает расходников»
- создание supply_alert и уведомление админу

Чекпоинт:
- алерт уходит с перечнем: моющее/губки/тряпки/прочее

## Этап 6 — SLA monitor + escalation
Задачи:
- периодический SLA monitor job
- эскалация по молчанию
- антиспам

Чекпоинт:
- таймауты реально ловятся и шлют SOS

## Этап 7 — Admin controls
Задачи:
- команды/кнопки для ручного менеджмента задач
- просмотр проблемных задач

Чекпоинт:
- админ может разрулить любую зависшую задачу

## Этап 8 — QA + hardening
Задачи:
- unit + integration tests
- нагрузочный smoke (много задач)
- логирование и финальные фиксы

Чекпоинт:
- стабильный прогон тестов
- DoD закрыт

---

## 9) Definition of Done (для утверждения)

- [ ] На каждый релевантный выезд создаётся CleaningTask
- [ ] Уборщица ведёт задачу по этапам: accepted → in_progress → done
- [ ] Чеклист обязателен, включая «заправила постели / обновила моющее / губки / тряпки»
- [ ] Фото-отчёт обязателен (мин. 3 фото)
- [ ] По молчанию/срыву SLA есть автоматическая эскалация
- [ ] По расходникам уходит отдельный алерт админу
- [ ] Админ может вручную управлять проблемными задачами
- [ ] Есть тесты и базовая операционная наблюдаемость

---

## 10) Открытые вопросы перед стартом реализации

1. Один домик = одна уборщица по умолчанию или общий пул?
2. Фото хранить только как `telegram_file_id` или дублировать в файловое хранилище?
3. Нужна ли подпись/комментарий к каждому фото?
4. Список расходников фиксированный или редактируемый в настройках?
5. Кому эскалировать в первую очередь: owner, admin, обоим сразу?

---

## 11) Рекомендуемый порядок внедрения (без риска)

`Этап 1 -> 2 -> 3 -> 4 -> 6 -> 5 -> 7 -> 8`

Почему так:
- сначала делаем стабильную задачу и переходы,
- потом качество (чеклист/фото),
- затем SLA,
- и уже на этом фундаменте закупочные алерты + админские инструменты.

---

Если этот roadmap утверждаем, следующий шаг — разложу его в **Issue backlog** (по 1 issue на этап/подэтап) с estimate и acceptance criteria.
