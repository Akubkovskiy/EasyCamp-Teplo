# SaaS Transformation Roadmap

## 1. Стратегическая цель продукта
Мы переводим EasyCamp из “кастомной разработки под один объект” в single‑tenant white‑label систему для баз отдыха: один код, много независимых инстансов, каждым со своей БД, Avito, ботом и брендингом.

### Ключевые принципы:
*   **Один источник правды — БД** (дома, брони, настройки, контакты, интеграции).
*   **.env — только техника** (DSN БД, режимы, секреты bootstrap), всё бизнес‑специфичное хранится и редактируется через UI.
*   **Самообслуживание**: Владелец базы может сам “завести” свой объект: через web‑мастер и/или Telegram‑админку, без ручных SQL/правки кода.
*   **Engineering Standards**: Единое ядро, понятные миграции (Alembic), наблюдаемость (JSON‑логи, метрики) и предсказуемый деплой.

## 2. Архитектурные опорные точки
*   **Single‑tenant**: каждая база = отдельный деплой/инстанс (своё .env, своя БД, свой домен).
*   **Multi‑interface**: один и тот же доменный слой (services, модели) обслуживает:
    *   Telegram‑бот (гости + быстрый админ UX).
    *   Веб‑админка (основное управление, onboarding, календарь).
    *   Интеграции (Avito, Google Sheets).
*   **Observability**: JSON-логи, Middlewares, Rate Limiting, Health Checks.

## 3. Roadmap по фазам

### ✅ Фаза A. Завершение де‑брендинга и консолидация конфигов
Цель: убедиться, что ни один слой не тянет “Teplo/Архыз” из кода.
*   **Analytics**: Сводная таблица конфигурируемых полей.
*   **Backend**: Messages/Settings интерфейсы, удаление хардкода.
*   **QA**: Smoke-test с нейтральными данными.
*   **Status**: DONE (Verified).

### 🚀 Фаза B. Веб‑админка: каркас + аутентификация
Цель: дать устойчивую точку входа под web‑управление.
*   **Backend**:
    *   Новые роуты `/admin-web`.
    *   Базовая авторизация (Cookies/Session).
    *   Роли (Owner/Admin).
*   **Frontend**:
    *   SSR (Jinja2) для быстрого старта.
    *   Страница логина.
    *   Дашборд-заглушка.
*   **QA**: Login/Logout flow, security checks.

### Фаза C. Setup Wizard (Onboarding)
Цель: убрать ручной onboarding, "Self-hosted" опыт.
*   **Backend**: 
    *   Редирект на `/setup` если БД пустая.
    *   `SETUP_SECRET` для защиты.
    *   Инициализация настроек через форму.
*   **Frontend**: Wizard UI (Steps).
*   **QA**: Full setup flow from zero.

### Фаза D. Управление домами + медиаконтент
Цель: перенести управление контентом в Веб.
*   **Backend**: REST CRUD для `House` и `HousePhoto`.
*   **Frontend**: Таблицы, Формы редактирования, Загрузка фото.

### Фаза E. Календарь бронирования в админке
Цель: Визуальный контроль загрузки.
*   **Backend**: API `/calendar` (диапазон дат, статусы).
*   **Frontend**: Сетка календаря, статусы, детали брони.

### Фаза F. Роли, безопасность, эксплуатация
Цель: Production readiness.
*   **Security**: RBAC, Secret mgmt.
*   **Ops**: Runbooks, Scaling scripts.

## 4. Связь с Ботом и Sheets
*   **Бот**: Потребляет настройки из БД (которые правит Веб). Остается "мобильным пультом".
*   **Google Sheets**: Вторичный интерфейс (бэкап/аналитика). Настраивается тоже через Веб.
